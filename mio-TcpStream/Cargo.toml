[package]
name = "mio_tcp_stream"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"
mio = { path = "../../mio", features = ["net", "os-poll"] }